#JSGF V1.0;

/**
 * JSGF Digits Grammar file
 */

grammar control;

public <miscellaneous> = (quit);

public <frases> = (hello);

public <commands> = Turn <directions>;

public <directions> = (left | right);
